# NeMo Guardrails Configuration
# Defines the LLM, rails, and behavior for the Financial Analyst RAG system

models:
  - type: main
    engine: groq
    model: llama-3.3-70b-versatile

# Enable input and output rails
rails:
  input:
    flows:
      - check financial topic
  output:
    flows:
      - check output for hallucination